STL
	最初由HP实验室开发,STL具有工业强度的高效C++库
	体现了泛型编程，引入了迭代器，需求，概念，模型，容器等
	三大类
		算法
		容器 
		迭代器

	容器是数据结构,容器提供迭代器,算法通过迭代器指定一个范围操作容器


C++传值，传地址(指针)，传引用.
	this book say 引用是一个别名，没有分配地址,即编译器会处理它，将它转化为实际引用的变量，对引用的变量修改会直接影响原对象.
	but 在c++反汇编分析一书中VC++6.0环境下，传引用被转化为了指针


C++
	*p++=*(p++) 因为++和*优先级相同但是遵循右结合性。先p指向运算,然后p++
	*(++p) is 先++后*
